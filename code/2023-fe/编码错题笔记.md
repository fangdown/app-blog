# 0504

- single 是一个自执行匿名函数， 写成了一个普通构造函数
- 发布订阅模式 增加一下 event[type]存在的判断
- mypromise 需要在 resolve 和 reject 函数里判断 if(this.status !== pending) return
- 快排 arr.length <2 return arr ;concat 合并单个值或者一个数组，返回新数组

- debounce 先删后建， throttle 先删， 判断剩余时间，无剩余时间立即执行，有则用剩余时间 ， 需用到 apply
- 千分位正则 /(\d)()+$/g (?=(?:\d{3})+$) , 继续练习
