---
title: 说说对选择排序的理解
date: 2021-10-20 00:00:00
tags: 
  - 算法
categories: 
  - 算法
permalink: /pages/4a89c5/
---

## 是什么
选择排序： 分最大或最小原则去排序，
- 从未排序中选择最小的数放入已排序起始
- 从未排序中选择最小的数放入到已排序末尾

![](https://api.git123.cn/proxy/image?url=https://img-blog.csdnimg.cn/img_convert/dbb60164b15e22c0571aa19dfd4aa0de.gif)
## 怎么用
```js
function select(arr) {
  const len = arr.length;
  let minIndex, temp;
  for (let i = 0; i < len - 1; i++) {
    minIndex = i;
    for (let j = i + 1; j < len - 1; j++) {
      if (arr[j] < arr[minIndex]) {
        minIndex = j;
      }
    }
    temp = arr[i];
    arr[i] = arr[minIndex];
    arr[minIndex] = temp;
  }
  return arr
}
const arr = [1, 19, 2, 5, 99, 23];
const res = select(arr);
console.log(res); // [ 1, 2, 5, 19, 99, 23 ]

```
## 场景
时间复杂度高， 不推荐使用
