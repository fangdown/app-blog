---
title: 说说什么是尾递归及其应用
date: 2021-09-01 00:00:00
tags: 
  - js
categories: 
  - js
permalink: /pages/2fefc1/
---

## 是什么

- 递归：在函数内部调用自身
- 尾递归： 在函数尾部调用自身

## 递归

> 复杂度 O(n)

```js
function factorial(n) {
  if (n === 1) return 1;
  return n * factorial(n - 1);
}
factorial(5, 6); // 120
```

## 尾递归

> > 复杂度 O(1)

```js
function factorial(n, total) {
  if (n === 1) return total;
  return factorial(n - 1, n * total);
}
factorial(5, 6); // 120
```

## 应用场景

- 数组求和

```js
function sum(arr, total) {
  if (arr.length === 1) {
    return total;
  }
  return sum(arr, total + arr.pop());
}
```

- 斐波那契

```js
function fb(n, start = 1, total = 1) {
  if (n <= 2) {
    return total;
  }
  return fb(n - 1, total, total + start);
}
```
![](https://api.git123.cn/proxy/image?url=https://mmbiz.qpic.cn/mmbiz_png/gH31uF9VIibTLPXqfAd5M8ZicLibDgBGSTR8y7YvUzV342V59cyjtmG0IlasJ04pDFSb3B1Xu6219Hia8vVOY7hqzQ/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1)