---
title: 说说nodejs中间件是什么，如何封装一个中间件
date: 2021-08-16 00:00:00
tags: 
  - 中间件
categories: 
  - nodejs
permalink: /pages/2af22e/
---

## 是什么
在nodejs中，中间件本质是一个回调函数，参数包括了请求对象，响应对象和执行下一个中间件的next函数
> foo(ctx, next)
## 怎么用
- 日志模块
```js
module.exports = async (ctx, next) =>{
  const startTime = Date.now()
  const requestTime = new Date()
  await next()
  const ms = Date.now() - startTime
  let logout = `${ctx.request.ip}---${requestTime} -- ${ctx.method} --${ctx.url}--${ctx.ms}ms`
  fs.appendFileSync('./log.txt', logout+'\n')
}
```
- token校验
```js
module.exports = async (ctx, next) => {
  const token = ctx.header.token
  if(token){
    try{
      await verify(token)
    }catch(err){
      throw err
    }
  }
  await next()
}
```

## 原理
中间件应职责单一，应有扩展性和组合性
## FAQ

![](https://api.git123.cn/proxy/image?url=https://mmbiz.qpic.cn/mmbiz_png/gH31uF9VIibT89icfGPDtvroiaaWBc9B1Yic6xsvrULNqcKucsG4hnYlibdkmBvu6icaafcUicNvvottdhDz8x7WBmVKw/640?wx_fmt=png&tp=webp&wxfrom=5&wx_lazy=1&wx_co=1)